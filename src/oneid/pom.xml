<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>it.pagopa.oneid</groupId>
    <artifactId>oneid</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <compiler-plugin.version>3.12.1</compiler-plugin.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.version>3.10.1</quarkus.platform.version>
        <skipITs>true</skipITs>
        <surefire-plugin.version>3.2.5</surefire-plugin.version>
        <it.pagopa.oneid.common.group-id>it.pagopa.oneid.common</it.pagopa.oneid.common.group-id>
        <it.pagopa.oneid.common.client.artifact-id>client</it.pagopa.oneid.common.client.artifact-id>
        <it.pagopa.oneid.common.client.version>1.0.0-SNAPSHOT</it.pagopa.oneid.common.client.version>
        <it.pagopa.oneid.common.model.artifact-id>model</it.pagopa.oneid.common.model.artifact-id>
        <it.pagopa.oneid.common.model.version>1.0.0-SNAPSHOT</it.pagopa.oneid.common.model.version>
        <opensaml.version>4.3.0</opensaml.version>
        <aws.java.sdk.version>2.25.63</aws.java.sdk.version>

    </properties>
    <repositories>
        <repository>
            <id>shib-build-releases</id>
            <name>Shibboleth Build Releases Repository</name>
            <url>https://build.shibboleth.net/nexus/content/repositories/releases/</url>
        </repository>
    </repositories>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-core</artifactId>
                <version>${opensaml.version}</version>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-saml-api</artifactId>
                <version>${opensaml.version}</version>
                <scope>compile</scope>
            </dependency>
            <dependency>
                <groupId>org.opensaml</groupId>
                <artifactId>opensaml-saml-impl</artifactId>
                <version>${opensaml.version}</version>
                <scope>compile</scope>
            </dependency>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.java.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>1.18.32</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>${it.pagopa.oneid.common.group-id}</groupId>
                <artifactId>${it.pagopa.oneid.common.client.artifact-id}</artifactId>
                <version>${it.pagopa.oneid.common.client.version}</version>
            </dependency>
            <dependency>
                <groupId>${it.pagopa.oneid.common.group-id}</groupId>
                <artifactId>${it.pagopa.oneid.common.model.artifact-id}</artifactId>
                <version>${it.pagopa.oneid.common.model.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>native</name>
                </property>
            </activation>
            <properties>
                <skipITs>false</skipITs>
                <quarkus.native.enabled>true</quarkus.native.enabled>
            </properties>
        </profile>
        <profile>
            <id>oneid-ecs-core-aggregate</id>
            <modules>
                <module>oneid-common</module>
                <module>oneid-ecs-core</module>
            </modules>
        </profile>
        <profile>
            <id>oneid-lambda-client-registration-aggregate</id>
            <modules>
                <module>oneid-common</module>
                <module>oneid-lambda-client-registration</module>
            </modules>
        </profile>
        <profile>
            <id>oneid-lambda-service-metadata-aggregate</id>
            <modules>
                <module>oneid-common</module>
                <module>oneid-lambda-service-metadata</module>
            </modules>
        </profile>
        <profile>
            <id>oneid-all</id>
            <modules>
                <module>oneid-common</module>
                <module>oneid-ecs-core</module>
                <module>oneid-lambda-client-registration</module>
                <module>oneid-lambda-service-metadata</module>
            </modules>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>
    </profiles>
    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
            </plugin>
            <plugin>
                <groupId>org.jboss.jandex</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>1.2.1</version>
                <executions>
                    <execution>
                        <id>make-index</id>
                        <goals>
                            <goal>jandex</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
